---
title: "Análisis Exploratorio de Datos de la Premier League 2021/2022"
author:
  - "Javier Ugena García"
  - "Gabriel Carlos Suárez Chiquito"
  - "Bruno Guedeja-Marrón Ramos"
date: today
lang: es
format: 
  html:
    lang: es
    toc: true
    toc_depth: 3
    theme: cosmo
categories:
  - Inferencia Estadística
  - Grado en Ciencia e Ingenieria de Datos
execute:
  warning: false
  echo: true
---

## 1. Introducción y Objetivos.

Esta práctica marca el primer contacto real con un Análisis Exploratorio de Datos (EDA) completo y sistemático , el cual es una fase fundamental en la ciencia de datos.

El objetivo principal de este informe es aplicar técnicas de estadística descriptiva y visualización en el dataset de la Premier League para comprender en profundidad su estructura, identificar patrones, detectar anomalías, y finalmente, formular hipótesis que serán validadas en futuras etapas de inferencia estadística.

## 2. Presentación y Descripción del Dataset.

### 2.1. Contexto y Origen de los Datos.

El conjunto de datos utilizado en esta práctica contiene estadísticas detalladas de jugadores de la Premier League (la máxima división de fútbol en Inglaterra) correspondientes a la temporada 2021/2022.

Estos datos buscan describir el rendimiento deportivo y las características demográficas de los jugadores en la competición. Su origen es Kaggle y han sido recopilados con el fin de analizar variables clave como el número de goles, minutos jugados, pases realizados, y la posición de los jugadores.

### 2.2. Carga y Dimensiones del Dataset.

A continuación, cargamos las librerías necesarias y el conjunto de datos de la Premier League para verificar sus dimensiones (número de observaciones y variables).

```{r}

library(tidyverse)
library(dplyr)
library(summarytools) 
library(GGally) 
library(gt)
library(flextable)
library(knitr) 
library(corrplot)
library(moments)
library(skimr)

datos <- read.csv("datos/datospremier.csv")

dimensiones <- dim(datos)

glimpse(datos)

```

### 2.3. Descripción Detallada de las Variables.

A continuación, se presenta una tabla que detalla cada variable del dataset, incluyendo su tipo estadístico, significado y unidades de medida. Esta descripción es crucial para un análisis riguroso.

| Variable | Tipo Estadístico | Descripción                         | Unidades de Medida        |
|----------|------------------|-------------------------------------|---------------------------|
| Player   | Categórica       | Nombre del jugador                  | N/A                       |
| Team     | Categórica       | Nombre del equipo                   | N/A                       |
| Nation   | Categórica       | Nacionalidad del jugador            | N/A                       |
| Pos      | Categórica       | Posición del jugador                | N/A                       |
| Age      | Numérica         | Edad del jugador                    | Años                      |
| MP       | Numérica         | Partidos jugados                    | Minutos                   |
| Starts   | Numérica         | Partidos iniciados de titular       | Cantidad de partidos      |
| Min      | Numérica         | Minutos jugados                     | Minutos                   |
| X90s     | Numérica         | Minutos jugados dividido por 90 minutos     | Cantidad de partidos      |
| Gls      | Numérica         | Goles anotados                      | Cantidad de goles         |
| Ast      | Numérica         | Asistencias realizadas              | Cantidad de asistencias   |
| G.PK     | Numérica         | Goles anotados sin contar penaltis  | Cantidad de goles         |
| PK       | Numérica         | Penaltis anotados                   | Cantidad de penaltis      |
| PKatt    | Numérica         | Penaltis intentados                 | Cantidad de penaltis      |
| CrdY     | Numérica         | Tarjetas amarillas                  | Cantidad de tarjetas      |
| CrdR     | Numérica         | Tarjetas rojas                      | Cantidad de tarjetas      |
| Gls.1    | Numérica         | Goles anotados por 90 minutos       | Goles por 90 minutos      |
| Ast.1    | Numérica         | Asistencias por 90 minutos          | Asistencias por 90 minutos|
| G.A      | Numérica         | Goles + Asistencias                 | Cantidad total            |
| G.PK.1  | Numérica         | Goles sin contar penaltis | Cantidad total        |
| G.A.PK   | Numérica         | Goles + Asistencias sin contar penaltis | Cantidad total    |
| xG       | Numérica         | Goles esperados                     | Cantidad de goles         |
| npxG     | Numérica         | Goles esperados sin contar penaltis | Cantidad de goles         |
| xA       | Numérica         | Asistencias esperadas               | Cantidad de asistencias   |
| npxG.xA | Numérica         | Goles esperados + Asistencias esperadas sin contar penaltis | Cantidad total    |
| xG.1 | Numérica         | Goles esperados por 90 minutos      | Goles por 90 minutos      |
| xA.1 | Numérica         | Asistencias esperadas por 90 minutos| Asistencias por 90 minutos|
| xG.xA    | Numérica         | Goles esperados + Asistencias esperadas | Cantidad total    |
| np.xG.1 | Numérica         | Goles esperados sin contar penaltis por 90 minutos | Goles por 90 minutos      |
| np.xG.xA.1 | Numérica         | Goles esperados + Asistencias esperadas sin contar penaltis por 90 minutos | Cantidad total    |

## 3. Análisis Univariante y Calidad de Datos.

El Análisis Univariante se centra en describir cada variable de forma aislada para comprender su distribución y detectar posibles problemas en los datos.

### 3.1. Detección y Tratamiento de Problemas de Calidad.

#### 3.1.1. Correción de detalles en los datos.

Viendo la base de datos, hemos detectado que hay variables como por ejemplo "MP" (Minutos Jugados) que tienen "," para el valor mil. Por lo que hay que eliminar las "," para que R lo pueda interpretar como numérico y no como carácter. También, podemos observar que la variable "Nation" tiene caracteres especiales. Además, las variables Team, Nation y Pos deberían ser de tipo factor ya que son variables categóricas.

Estrategia: Usaremos código R para corregir estos detalles en los datos.

```{r}

# Convertir la columna edad que contiene comas a numérica.

datos$Min <- as.numeric(gsub(",","", datos$Min))

# Corregir la variable Nation

datos$Nation <- iconv(datos$Nation, from = "latin1", to = "UTF-8")
datos$Nation <- str_replace_all(datos$Nation, "[^A-Za-z]", "")

# Convertir las variables categóricas a factor

datos$Team <- as.factor(datos$Team)
datos$Nation <- as.factor(datos$Nation)
datos$Pos <- as.factor(datos$Pos)

```

```{r}
# Verificar que se han corregido los datos

str(datos)

vars_numeric <- names(datos)[sapply(datos, is.numeric)]

tabla_rangos <- datos %>%
  summarise(across(
    all_of(vars_numeric),
    list(
      Mínimo = \(x) min(x, na.rm = TRUE),
      Máximo = \(x) max(x, na.rm = TRUE)
    )
  )) %>%
  tidyr::pivot_longer(
    cols = everything(),
    names_to = c("Variable", ".value"),
    names_sep = "_"
  )
kable(tabla_rangos,
      caption = "Tabla de valores mínimos y máximos de variables numéricas",
      digits = 2)
```

Observando la base de datos, hemos tomado la decisión de eliminar algunas variables que no aportan valor al análisis, como por ejemplo las variables que representan métricas por 90 minutos jugados (X90s, G.PK.1, xG.1, xA.1, npxG.1, npxG.xA.1). Estas variables son derivadas de las métricas totales y pueden generar redundancia en el análisis. Además de generar confusión al interpretar los resultados.

```{r}
# Eliminar las variables innecesarias

datos <- datos %>% select(-c(X90s, G.PK.1, xG.1, xA.1, npxG.1, npxG.xA.1))
```

La base de datos ya está corregida y lista para el análisis posterior. Quedaría tal que así:

```{r}

head(datos)

```

Con dimensiones:

```{r}

dim(datos)


```


#### 3.1.2. Valores Faltantes.

La identificación y el tratamiento de los valores faltantes (NA o similares) es un requisito clave.

Estrategia: Usaremos código R para inspeccionar qué variables tienen missings y en qué proporción, para luego decidir el tratamiento.

```{r}

# Contar el total de valores faltantes por columna
missings_por_columna <- datos %>%
  summarise_all(~sum(is.na(.))) %>%
  pivot_longer(everything(), names_to = "Variable", values_to = "N_Faltantes") %>%
  filter(N_Faltantes > 0) %>%
  mutate(Proporcion = N_Faltantes / nrow(datos) * 100)

print(missings_por_columna)

```

Analizando todos los missing values. Hemos detectado que hay 4 jugadores sin edad registrada. Los 144/145 valores faltantes en las demás columnas, se deben a que son jugadores que no han disputado minutos en la temporada, por lo que no tienen datos asociados a rendimiento.

Revisando cuidadosamente el dataset, hemos decidido que excepto los 4 valores de la edad, vamos a cambiar los valores faltantes detectados como N/A a 0. Ya que son variables numéricas.

Con los valores faltantes de edad, vamos a cambiar la edad por la mediana de edad de los jugadores para no perder esas observaciones y no alterar la distribución de la variable.

Estrategia: Usaremos código R para realizar estos tratamientos.

```{r}
# Reemplazar los valores faltantes en variables numéricas por 0

vars_numeric <- names(datos)[sapply(datos, is.numeric)]
for (var in vars_numeric) {
  datos[[var]][is.na(datos[[var]])] <- 0
}
# Reemplazar los valores faltantes en la variable 'Age' por la mediana
mediana_edad <- median(datos$Age, na.rm = TRUE)
datos$Age[is.na(datos$Age)] <- mediana_edad

# Verificar que no hay más valores faltantes

colSums(is.na(datos))

```

#### 3.1.3. Detección y Tratamiento de Duplicados.

Comprobaremos si existen filas duplicadas en el dataset y tomaremos las medidas necesarias para tratarlas.

```{r}

# Verificar duplicados

duplicados <- datos[duplicated(datos), ]
print(duplicados)

```

## 3.2. Análisis Descriptivo de variables numéricas.

Para las variables más comunes es necesario realizar un análisis descriptivo completo, que incluya tanto estadísticos como visualizaciones.

```{r}

stats_goals <- datos %>%
  filter(!is.na(Gls)) %>% 
  summarise(
    N_Total = n(),
    Media = mean(Gls),
    Mediana = median(Gls),
    Desviacion_Tipica = sd(Gls),
    RIQ = IQR(Gls),
    Asimetria = moments::skewness(Gls),
    Curtosis = moments::kurtosis(Gls)
  )

print(stats_goals)

```

A simples vista, la variable Gls (Goles anotados) presenta una distribución asimétrica positiva, con una media mayor que la mediana, lo que indica que hay algunos jugadores con un número muy alto de goles que están influyendo en la media. La desviación típica es relativamente alta, lo que sugiere una gran variabilidad en el número de goles anotados por los jugadores.

Vamos a verlo un poco más visual:

```{r}

#| label: resumen_numericas
#| echo: true

# skim() genera automáticamente un resumen exhaustivo que incluye:
# - N faltantes (missings)
# - N completo
# - Media, Desviación Estándar (sd)
# - Cuartiles (p25, p50/Mediana, p75)
# - Histograma ASCII
# - Asimetría (en la sección 'numeric')
# - Y otros estadísticos por defecto para variables numéricas.

resumen_numericas <- datos %>%
  # Selecciona solo las columnas numéricas relevantes (omite IDs, etc.)
  select_if(is.numeric) %>%
  skim()

print(resumen_numericas)

```


# TENERIFE


# 2. Presentación y Descripción del Dataset

## Introducción y definición de objetivos

El estudio con los datos obtenidos de la Premier League tiene como objetivo principal analizar y comprender el rendimiento de los jugadores en la liga de fútbol más competitiva del mundo. A través de este análisis, se pretende identificar patrones y tendencias que puedan influir en el desempeño de los jugadores, así como evaluar su contribución al éxito de sus respectivos equipos.

Para esto se han recogido una serie de datos de los jugadores de la Premier League, incluyendo estadísticas individuales como goles, asistencias, minutos jugados, tarjetas amarillas y rojas, entre otros. Además, se han considerado variables contextuales como la posición del jugador, el equipo al que pertenece y su nacionalidad.

Los datos corresponden a la temporada 2021/2023 de la Premier League, y se han obtenido de kaggle.com, una plataforma reconocida por su amplia base de datos deportivos. La elección de esta fuente se debe a la calidad y fiabilidad de los datos proporcionados, así como a la facilidad de acceso para su análisis.

| Variable | Descripción                                                 |
|----------|-------------------------------------------------------------|
| Player   | Nombre del jugador                                          |
| Team     | Nombre del equipo                                           |
| Nation   | Nacionalidad del jugador                                    |
| Pos      | Posición del jugador                                        |
| Age      | Edad del jugador                                            |
| MP       | Minutos jugados                                             |
| Starts   | Partidos iniciados de titular                               |
| Min      | Minutos jugados                                             |
| Gls      | Goles anotados                                              |
| Ast      | Asistencias realizadas                                      |
| G.PK     | Goles anotados sin contar penaltis                          |
| PK       | Penaltis anotados                                           |
| PKatt    | Penaltis intentados                                         |
| CrdY     | Tarjetas amarillas                                          |
| CrdR     | Tarjetas rojas                                              |
| Gls.1    | Goles anotados por 90 minutos                               |
| Ast.1    | Asistencias por 90 minutos                                  |
| G.A      | Goles + Asistencias                                         |
| G.A.PK   | Goles + Asistencias sin contar penaltis                     |
| xG       | Goles esperados                                             |
| npxG     | Goles esperados sin contar penaltis                         |
| xA       | Asistencias esperadas                                       |
| xG.xA    | Goles esperados + Asistencias esperadas                     |
| np.xG.xA | Goles esperados + Asistencias esperadas sin contar penaltis |

Observamos el tipo de las variables

#### Dimensiones dataset

```{r}
dim(datos)
```

Observamos que hay un total 691 filas que corresponden a los jugadores y 24 columnas que son las variables del dataset.

#### Tipos de variables

Obeservamos los tipos de variables que hay en el dataset.

```{r}
str(datos)
```

Se observa que todas las variables estan con su tipo excepto la variable 'min' Que en el dataset la lee como char y realmente es de tipo numérica.Por otro lado la variable 'Nation' tiene carácteres especiales. Tambien se observa que las variables Team, Nation y Pos deberían ser de tipo factor ya que son variables categóricas.

Corregimos lo menvionado anteriormente de la variable 'Min' y 'Nation'

```{r}
datos$Min <- as.numeric(gsub(",","", datos$Min))
datos$Nation <- iconv(datos$Nation, from = "latin1", to = "UTF-8")
datos$Nation <- str_replace_all(datos$Nation, "[^A-Za-z]", "")
datos$Team <- as.factor(datos$Team)
datos$Nation <- as.factor(datos$Nation)
datos$Pos <- as.factor(datos$Pos)

str(datos)
```

Observamos que se ha corregido todo correctamente. Las variables Nation, Team y Pos son categoricas.

```{r}
vars_numeric <- names(datos)[sapply(datos, is.numeric)]

tabla_rangos <- datos %>%
  summarise(across(
    all_of(vars_numeric),
    list(
      Mínimo = \(x) min(x, na.rm = TRUE),
      Máximo = \(x) max(x, na.rm = TRUE)
    )
  )) %>%
  tidyr::pivot_longer(
    cols = everything(),
    names_to = c("Variable", ".value"),
    names_sep = "_"
  )
kable(tabla_rangos,
      caption = "Tabla de valores mínimos y máximos de variables numéricas",
      digits = 2)
```

```{r}

par(mfrow = c(3, 3))  # 3 columnas x 3 filas

for (var in vars_numeric) {
  hist(datos[[var]],
       main = paste("Distribución de", var),
       xlab = var,
       col = "gold",
       border = "black")
}

```
```{r}
vars_factor <- names(datos)[sapply(datos, is.factor)]

for (var in vars_factor) {
  conteos <- table(datos[[var]])  # cuenta las categorías
  barplot(conteos,
          main = paste("Distribución de", var),
          xlab = var,
          ylab = "Frecuencia",
          col = "lightblue",
          border = "black",
          las = 2)  # gira etiquetas del eje X para que se lean mejor
}

# Restaurar la configuración
par(mfrow = c(1, 1))
```


# BRUNO
#3. Análisis Univariante y Calidad de Datos (Paso 2)

##3.1. Tratamiento y Detección de Problemas de Calidad
    ###Valores Faltantes (*Missing Values):
        ####Identificación de variables con valores faltantes y su proporción.
        
```{r}
colSums(is.na(datos))
datos[rowSums(is.na(datos)) > 0, ]
```
        Hay 4 erratas de edad de las cuales en 3 de ellas no sale ni la edad ni el país, por lo que se eliminarán esas filas.  
        ####Análisis del patrón de los missings.
        ####Decisión y Justificación del Tratamiento (imputación, eliminación, etc.).
    ###Inconsistencias y Errores:
        ####Verificación de consistencia de valores y formatos.
        ####Detección y tratamiento de duplicados.

##3.2. Análisis Descriptivo por Tipo de Variable*
    ###Variables Numéricas: Para cada variable relevante:
        * [cite_start]Cálculo de Estadísticos: Media, Mediana, Moda [cite: 31][cite_start], Desviación Típica, Rango Intercuartílico [cite: 32][cite_start], Asimetría, Curtosis [cite: 33][cite_start], Cuartiles[cite: 34].
        * [cite_start]Visualizaciones: *Histogramas, **Diagramas de Caja (*Boxplots)**, Gráficos de Densidad, Gráficos Q-Q[cite: 35].
    * *Variables Categóricas:* Para cada variable relevante:
        * [cite_start]Tablas de Frecuencia (absolutas y relativas)[cite: 37].
        * [cite_start]Estadísticos: Moda y distribución[cite: 38].
        * [cite_start]Visualizaciones: *Gráficos de Barras*, Gráficos de Sectores (solo si hay pocas categorías)[cite: 39].

